# ─── Saubh.Tech API — Environment Variables ─────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env — it contains real secrets.
# ─────────────────────────────────────────────────────────────────────────────

# ─── Database (matches Docker dev stack in infra/compose/dev/) ───────────────
DATABASE_URL="postgresql://saubhtech:change_me_postgres_dev@localhost:5432/saubhtech?schema=public"
